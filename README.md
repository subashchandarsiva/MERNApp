# MERNApp

This repository contains dependency manifests only. There is no application
entry point or implemented application test suite yet.

Use Node.js 22 or newer and `npm ci` to install the locked Express and
jsonwebtoken dependencies. Run `npm audit` to inspect dependency advisories.

See [MAINTENANCE.md](MAINTENANCE.md) for validation and update policy.
